---
import Section from "@/components/Section.astro";
import { education } from "@cv";

---
<Section title = "Educación">
    <ul>
        {education.map(({ institution, url, area, studyType, startDate, endDate }) => {
            const startYear = new Date(startDate).getFullYear();
            const simbol = endDate ? "-" : "";
            const endYear = new Date(endDate).getFullYear();
            return(
            <li>
                <article>
                    <header>
                        <div>
                            <h3>{institution}</h3>
                            <h4>{area}</h4>
                            <span>{studyType}</span>
                        </div>
                        <time>{startYear} {simbol} {endYear} </time>
                    </header>
                    <footer>
                        
                    </footer>
                </article>
            </li>
            )
        })}
    </ul>
</Section>

<style>
    ul {
      display: flex;
      flex-direction: column;
      gap: 32px;
    }
  
    article h3 {
      font-weight: 500;
      color: #111;
    }
  
    article h4 {
      color: #222;
      font-weight: 400;
    }
    
    span{
      font-size: 14px;
      color: #555;
    }
    header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 4px;
    }
  
    time {
      color: #555;
      font-size: 0.85rem;
    }
  </style>